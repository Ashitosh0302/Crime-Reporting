<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Missing Items Reporting</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        *{margin:0;padding:0;box-sizing:border-box;}

        body{
            font-family:'Segoe UI',Arial,sans-serif;
            background:linear-gradient(135deg,#1e3c72,#2a5298,#0f2027);
            color:#fff;
            min-height:100vh;
            padding:20px 10px 30px;
        }

        .header-logos{
            text-align:center;
            margin-bottom:10px;
        }

        .header-logos img{
            height:120px;
            margin:0 8px;
        }

        .title{
            text-align:center;
            font-size:1.9rem;
            margin-top:8px;
        }

        .subtitle{
            text-align:center;
            font-size:1rem;
            opacity:0.9;
            margin-top:4px;
            margin-bottom:16px;
        }

        .container{
            max-width:900px;
            margin:0 auto;
            display:grid;
            grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
            gap:22px;
            padding:0 8px;
        }

        .card{
            background:rgba(255,255,255,0.08);
            border-radius:16px;
            padding:22px 18px;
            box-shadow:0 8px 25px rgba(0,0,0,0.35);
            backdrop-filter:blur(10px);
            text-align:center;
            transition:transform .3s ease,box-shadow .3s ease,background .3s ease;
            display:flex;
            flex-direction:column; /* for button alignment */
        }

        .card:hover{
            transform:translateY(-6px);
            box-shadow:0 16px 32px rgba(0,0,0,0.45);
            background:rgba(255,255,255,0.16);
        }

        .card img{
            width:90px;
            height:90px;
            border-radius:50%;
            object-fit:cover;
            margin:0 auto 14px;
            border:3px solid rgba(226,103,21,0.7);
        }

        .card h3{
            margin-bottom:8px;
            font-size:1.1rem;
        }

        .card p{
            font-size:0.9rem;
            opacity:0.9;
        }

        .card-actions{
            margin-top:auto; /* push button area to bottom */
            margin-top:18px;
            display:flex;
            justify-content:center;
        }

        .card form{
            margin:0;
        }

        .btn-register{
            position:relative;
            padding:10px 22px;
            border-radius:999px;
            border:none;
            outline:none;
            cursor:pointer;
            background:linear-gradient(45deg,#e26715,#f4a261);
            color:#fff;
            font-weight:600;
            font-size:0.85rem;
            text-transform:uppercase;
            letter-spacing:0.5px;
            overflow:hidden;
            box-shadow:0 4px 15px rgba(226,103,21,0.5);
            transition:transform .15s ease,box-shadow .15s ease;
            white-space:nowrap;
        }

        .btn-register::after{
            content:"";
            position:absolute;
            width:0;
            height:0;
            top:50%;
            left:50%;
            background:rgba(255,255,255,.4);
            border-radius:50%;
            transform:translate(-50%,-50%);
            transition:width .35s ease,height .35s ease,opacity .4s ease;
            opacity:0;
        }

        .btn-register:hover{
            transform:translateY(-2px);
            box-shadow:0 8px 20px rgba(226,103,21,0.7);
        }

        .btn-register:active{
            transform:translateY(1px) scale(.97);
            box-shadow:0 2px 8px rgba(0,0,0,0.6);
        }

        .btn-register.clicked::after{
            width:240px;
            height:240px;
            opacity:0;
        }

        footer{
            text-align:center;
            font-size:0.85rem;
            opacity:0.85;
            margin-top:26px;
        }

        @media(max-width:600px){
            .header-logos img{height:95px;}
            .title{font-size:1.6rem;}
            .card{padding:20px 16px;}
        }
    </style>
</head>
<body>
    <div class="header-logos">
        <img src="images/government_of_india.jpg" alt="Government of India">
        <img src="images/mh_police.jpg" alt="Maharashtra Police">
    </div>
    <h1 class="title">Missing Items Reporting</h1>
    <p class="subtitle">File a report for lost gadgets, vehicles, or other belongings</p>

    <div class="container">

        <div class="card">
            <img src="images/mobile missing.jpg" alt="Missing Gadget">
            <h3>Missing Gadget Report</h3>
            <p>Report lost mobiles, laptops, tablets and other electronic gadgets.</p>
            <div class="card-actions">
                <form action="/Missing_Item_forms/Missing_gadgets" method="get">
                    <button type="submit" class="btn-register">
                        Register / नोंदणी करा
                    </button>
                </form>
            </div>
        </div>

        <div class="card">
            <img src="images/missing vehicle.png" alt="Missing Vehicle">
            <h3>Missing Vehicle Report</h3>
            <p>Submit details of missing bikes, cars or other vehicles.</p>
            <div class="card-actions">
                <form action="/Missing_Item_forms/Missing_vehicle" method="get">
                    <button type="submit" class="btn-register">
                        Register / नोंदणी करा
                    </button>
                </form>
            </div>
        </div>

        <div class="card">
            <img src="images/robbery.png" alt="Other Missing Items">
            <h3>Other Missing Items Report</h3>
            <p>Report other lost items like documents, jewelry, or valuables.</p>
            <div class="card-actions">
                <form action="/Missing_Item_forms/Other_Missing_item" method="get">
                    <button type="submit" class="btn-register">
                        Register / नोंदणी करा
                    </button>
                </form>
            </div>
        </div>

    </div>

    <footer>
        &copy; 2026 Maharashtra Police. All rights reserved.
    </footer>

    <script>
        // Optional: ripple effect on submit (without breaking navigation)
        document.querySelectorAll('.btn-register').forEach(btn=>{
            btn.addEventListener('click', ()=>{
                btn.classList.remove('clicked');
                void btn.offsetWidth;
                btn.classList.add('clicked');
            });
        });
    </script>
</body>
</html>
